#zj 
# ==**比赛中介**==
n天前有1场比赛  
中介如下：  
# [$\color{Blue} \mathcal{{\Huge 一、} }$](https://yuanyinoj.cn/d/yyzx007/contest/68961f1e45aaab22c6d569b6)
## 1.芝士问题
- 太久没做题，不会做题了
- 对模拟有亿点遗忘
- 思维有亿点定向
## 2.改进措施
- 多做题
## 3.题目分析
- 1.[时钟刻度](https://yuanyinoj.cn/d/yyzx007/p/BS0026?tid=68961f1e45aaab22c6d569b6)：太简单了！！！本题难了我好久，其实很简单。只需要AC即可。
```cpp
#include <bits/stdc++.h>
using namespace std;

int main () {
	int a, b;
	cin >> a >> b;
	if (a > b) cout << 12 - a + b;
	else cout << b - a;
}
```
- 2.[时钟刻度](https://yuanyinoj.cn/d/yyzx007/p/BS0026?tid=68961f1e45aaab22c6d569b6)：太简单了！！！本题难了我好久，其实很简单。只需要AC即可。至于我为什么是95分，这个……
```cpp
#include <bits/stdc++.h>
using namespace std;

int main () {
	int t;
	cin >> t;
	while (t--) {
		int a, b, c, num = 0;
		cin >> a >> b >> c;
		if (b >= a && b >= c) {
			num = min (max (b - a, b - c) + 1, b - 1);
			b -= num;
		}
		cout << num + ((a > b) ? 0 : b - a + 1) + ((c > b) ? 0 : b - c + 1) << '\n';
	}
}
```
- 3.[新工作日](https://yuanyinoj.cn/d/yyzx007/p/124?tid=68961f1e45aaab22c6d569b6)：太简单了！！！本题难了我好久，其实很简单。只需要AC即可。
```cpp
// 代码中"sb"的意思是"上班"，别瞎想！！！
#include <bits/stdc++.h>
using namespace std;
#define int long long
bool rn (int y) {
	return (y % 4 == 0 && y % 100 != 0) || (y % 400 == 0);
}
int sbr (int y, int m, int d) {
	const int sbd[] = {0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334, 365};
	int ans = 365 * (y - 1970);
	ans += (y - 1) / 4 - 1970 / 4;
	ans -= (y - 1) / 100 - 1970 / 100;
	ans += (y - 1) / 400 - 1970 / 400;
	ans += sbd[m - 1];
	if (m > 2 && rn (y)) ans++;
	ans += d;
	return ans;
}
int js (int a, int b, int c, int x, int y, int z) {
	int l = sbr (a, b, c);
	int r = sbr (x, y, z);
	if (l > r) { swap (a, x), swap (b, y), swap (c, z), swap (l, r); }
	int sb2 = (l - 1 + 4) % 7;
	int sbl = r - l + 1, fsb = sbl / 7, ans = fsb * 5;
	int rsb = sbl % 7;
	for (int i = 0; i < rsb; ++i) {
		int ss = (sb2 + i) % 7;
		if (ss == 3 || ss == 4 || ss == 5 || ss == 6 || ss == 0) { ans++; }
	}
	return ans;
}
signed main () {
	ios::sync_with_stdio (false);
	cin.tie (nullptr);
	int t;
	cin >> t;
	while (t--) {
		int a, b, c, x, y, z;
		cin >> a >> b >> c >> x >> y >> z;
		cout << js (a, b, c, x, y, z) << '\n';
	}
	return 0;
}
```
- 4.[与运算](https://yuanyinoj.cn/d/yyzx007/p/128?tid=68961f1e45aaab22c6d569b6)：太简单了！！！本题难了我好久，其实很简单。只需要AC即可。
```cpp
#include <iostream>
using namespace std;
#define int unsigned int
signed main () {
	ios::sync_with_stdio (false);
	cin.tie (0);
	int n, m, a, b, num = 0;
	cin >> n >> m;
	for (int i = 0; i < n; i++) {
		cin >> a;
		num |= a;
	}
	int ans = 0;
	for (int i = 0; i < m; i++) {
		cin >> b;
		if ((b & num) == num) { ans++; }
	}
	cout << ans;
	return 0;
}
```
- 5.[监控信号](https://yuanyinoj.cn/d/yyzx007/p/125?tid=68961f1e45aaab22c6d569b6)：太不简单了！！！本题难了我好久，其实很简单。思路有些复杂简写一下：用一个队列循环测试。只不过测试数量有点多！！！
```cpp
#include <bits/stdc++.h>
using namespace std;
int main () {
	int x, y;
	cin >> x >> y;
	vector<int> st (x + 1, 0);
	int f = 0;
	queue<pair<vector<int>, int>> q;
	int gr = (x + 7) / 8;
	for (int i = 0; i < gr; i++) {
		vector<int> g;
		for (int j = 0; j < 8; j++) {
			int id = i * 8 + j + 1;
			if (id <= x) g.push_back (id);
		}
		vector<int> fz = g;
		while (fz.size () < 8) fz.push_back (0);
		for (int j = 0; j < 8; j++) {
			if (j) cout << " ";
			cout << fz[j];
		}
		cout.flush ();
		string s;
		cin >> s;
		if (s == "ok") return 0;
		int k;
		cin >> k;
		if (k == g.size ()) {
			for (int id : g) {
				st[id] = 1;
				f++;
			}
		} else if (k == 0) {
			for (int id : g) st[id] = 2;
		} else {
			q.push ({g, k});
		}
	}
	while (!q.empty () && f < 8) {
		auto [g, to] = q.front ();
		q.pop ();
		if (g.empty ()) continue;
		int mid = g.size () / 2;
		vector<int> a (g.begin (), g.begin () + mid), b (g.begin () + mid, g.end ()), xz = a;
		while (xz.size () < 8) xz.push_back (0);
		for (int j = 0; j < 8; j++) {
			if (j) cout << " ";
			cout << xz[j];
		}
		cout.flush ();
		string s;
		cin >> s;
		if (s == "ok") return 0;
		int ka;
		cin >> ka;
		int kb = to - ka;
		if (ka == a.size ()) {
			for (int id : a) {
				st[id] = 1;
				f++;
			}
		} else if (ka == 0) {
			for (int id : a) st[id] = 2;
		} else {
			q.push ({a, ka});
		}
		if (kb == b.size ()) {
			for (int id : b) {
				st[id] = 1;
				f++;
			}
		} else if (kb == 0) {
			for (int id : b) st[id] = 2;
		} else {
			q.push ({b, kb});
		}
	}
	vector<int> ans;
	for (int i = 1; i <= x && ans.size () < 8; i++) {
		if (st[i] == 1) ans.push_back (i);
	}
	while (ans.size () < 8) ans.push_back (0);
	for (int i = 0; i < 8; i++) {
		if (i) cout << " ";
		cout << ans[i];
	}
	cout.flush ();
	string s;
	cin >> s;
	return 0;
}
```
- 6.[牛的反击](https://yuanyinoj.cn/d/yyzx007/p/BS0031?tid=68961f1e45aaab22c6d569b6)：太简单了！！！本题难了我好久，其实很简单。只需要AC即可。
```cpp
#include <iostream>
using namespace std;
int main () {
	ios_base::sync_with_stdio (false);
	cin.tie (nullptr);
	int t;
	cin >> t;
	while (t--) {
		long long a, b, p;
		cin >> a >> b >> p;
		if (p == 1) {
			cout << "YES\n";
			continue;
		}
		if (b == 1) {
			if ((p - 1) % a == 0) cout << "YES\n";
			else cout << "NO\n";
		} else {
			long long num = 1;
			bool f = 0;
			while (num <= p) {
				long long r = p - num;
				if (r == 0) {
					f = 1;
					break;
				}
				if (r > 0 && r % a == 0) {
					f = 1;
					break;
				}
				if (num > p / b) break;
				num = num * b;
			}
			cout << (f ? "YES\n" : "NO\n");
		}
	}
}
```
- 7.[猫咪编号](https://yuanyinoj.cn/d/yyzx007/p/127?tid=68961f1e45aaab22c6d569b6)：太不简单了！！！本题难了我好久，其实很简单。没AC可能是没开long long，~~**==但大概率是小隐数据的问题~==**~~
```cpp
#include <bits/stdc++.h>
using namespace std;
int main () {
	ios::sync_with_stdio (false);
	cin.tie (nullptr);
	int t;
	cin >> t;
	vector<vector<pair<int, int>>> mm;
	for (int i = 0; i < t; i++) {
		int n, m;
		cin >> n >> m;
		vector<pair<int, int>> v;
		if (n == 3) {
			v.push_back ({3, 2});
			v.push_back ({3, 2});
		} else if (n == 4) {
			v.push_back ({3, 4});
			v.push_back ({4, 2});
			v.push_back ({2, 4});
		} else {
			for (int i = 3; i < n; i++) { v.push_back ({i, n}); }
			int num = n;
			while (num > 2) {
				v.push_back ({n, 2});
				num = (num + 1) / 2;
			}
			v.push_back ({2, n});
		}
		mm.push_back (v);
	}
	for (auto i : mm) {
		cout << i.size () << "\n";
		for (auto p : i) { cout << p.first << " " << p.second << "\n"; }
	}
	return 0;
}
```

## 四、小提示
- 1.==**小提示：实力不行，打表来凑！！！**==
- 2.==**小提示：10年OI一场空，不开long long见祖宗！！！**==