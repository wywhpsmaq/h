#include<bits/stdc++.h>
using namespace std;
void js(int a) {
    int k = time(0);
    while (1) {
        if (time(0) == k + a) {
            return;
        }
    }
}
int main() {
    system("start 1.vbs");
    js(4);
    system("start 2.vbs");
    js(4);
    system("start 3.vbs");
}